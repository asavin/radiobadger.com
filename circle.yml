machine:
  node:
    version: 0.10
compile:
  - cd generate; npm install; cd ..
  - lsc generate/bin/generate.ls
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync out/ s3://radiobadger.com/ --delete
